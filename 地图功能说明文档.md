# 地图功能说明文档

## 概述

这是一个基于HTML、CSS和JavaScript开发的互动地图系统，提供了地点导航、角色互动、商店购物、背包管理和弹幕聊天等多种功能。该应用采用了响应式设计，支持自定义主题设置，并通过本地存储保存用户偏好。

## 文件结构

整个应用在单个HTML文件(`地图.html`)中实现，包含以下主要部分：
- HTML结构：定义了UI界面的布局
- CSS样式：定义了应用的视觉外观
- JavaScript：提供了交互逻辑和功能实现

## 主要功能

### 1. 地图导航系统
- 主要地点展示和选择
- 子地点展示和选择
- 位置移动确认机制
- 地点角色展示

### 2. 角色互动系统
- 角色信息展示
- 预定义互动选项
- 自定义互动输入
- 互动确认机制

### 3. 商店系统
- **地图数据解析商店物品列表 ([SHOP_ITEMS:])**
- **顶部导航栏添加商店按钮**
- 商品列表展示（名称、描述、价格）
- 购买功能（带数量选择和确认）
- 玩家金币显示 ([MONEY:])
- **购买后更新玩家金币和背包**

### 4. 背包系统
- **地图数据解析背包物品列表 ([INVENTORY:])**
- **顶部导航栏添加背包按钮**
- 物品列表展示
- **点击物品显示详情（名称、描述、可用操作）**
- 物品使用/丢弃功能（带确认）
- **操作后更新背包状态**

### 5. 弹幕聊天系统
- 弹幕发送功能
- 弹幕显示区
- 开关弹幕显示
- 关闭弹幕按钮

### 6. 游戏操作功能
- 等待时间（片刻/1小时）
- 时间跳转（时段/天）
- 角色互动快捷操作（发起对话、询问信息等）
- 邀请同行/约会功能

### 7. 设置系统
- 字体样式和大小调整
- 颜色主题自定义
- UI效果开关（阴影等）
- 预设颜色方案
- 设置保存和恢复

## 地图数据格式

地图系统使用特定的文本格式描述地图、角色和互动数据。这些数据通过文本解析并转换为可视化界面。

### 标签与格式

#### 头部标签
```
[TITLE: 地图标题]
[CONVERSING: 角色1,角色2]
[EXTERNAL_AREAS: 区域1,区域2]
[MOVEBLOCK: YES/NO]
[CURRENT_POS: X|Y]
[MONEY: 1000]
[SHOP_ITEMS: 物品1|价格|描述|类别,物品2|价格|描述|类别]
[INVENTORY: 物品1|描述|类别,物品2|描述|类别]
[BULLETS: 弹幕1;;弹幕2;;弹幕3]
```

#### 地点格式
```
[主要地点名称|X坐标|Y坐标|宽度|高度]
- 子地点1: 角色1{角色描述}, 角色2{角色描述}
- 子地点2: 角色3{角色描述}
```

#### 道路格式
```
[ROAD|X1,Y1;X2,Y2;X3,Y3|线宽|颜色]
```

### 数据结构说明

1. **地图数据(mapData)结构**：
   - `title`: 地图标题
   - `moveBlocked`: 移动限制状态
   - `locations`: 地点数组
   - `conversingPartners`: 当前对话伙伴
   - `externalAreas`: 外部区域列表
   - `currentUserPosition`: 用户当前位置
   - `roads`: 道路数据
   - `bullets`: 弹幕数据
   - `money`: 玩家金币
   - `shopItems`: 商店物品数组 (结构见下)
   - `inventoryItems`: 背包物品数组 (结构见下)

2. **地点(location)结构**：
   - `name`: 地点名称
   - `x`,`y`: 地点坐标
   - `width`,`height`: 地点尺寸
   - `subLocations`: 子地点数组

3. **子地点(subLocation)结构**：
   - `name`: 子地点名称
   - `characters`: 角色数组

4. **角色(character)结构**：
   - `name`: 角色名称
   - `description`: 角色描述(包含互动选项)

5. **商店物品(shopItem)结构**：
   - `id`: 物品ID (自动生成，例如 `shop-0`)
   - `name`: 物品名称
   - `price`: 物品价格
   - `description`: 物品描述
   - `category`: 物品类别

6. **背包物品(inventoryItem)结构**：
   - `id`: 物品ID (自动生成，例如 `inv-0`)
   - `name`: 物品名称
   - `description`: 物品描述
   - `category`: 物品类别
   - `actions`: 可用操作数组 (例如 `['使用', '丢弃']`) - **此字段目前为示例，需根据游戏逻辑扩展**

## 界面组件

### 顶部导航栏
- 设置按钮
- **商店按钮**
- **背包按钮**
- 弹幕聊天开关

### 地图区域
- 主要地点列表
- 子地点列表
- 当前位置显示
- 位置详情（包含角色）

### 互动面板
- 角色信息显示
- 互动选项按钮
- 自定义互动输入框

### 游戏操作区
- 当前伙伴显示
- 游戏操作按钮
- 前往地点确认按钮

### 弹幕聊天区
- 弹幕输入框
- 弹幕显示区
- 最小化按钮

### 模态框
- 设置面板
- **商店面板**
    - 玩家金币显示
    - 商品列表 (带名称、描述、价格、数量输入、购买按钮)
- **背包面板**
    - 物品列表
    - 物品详情区域 (点击物品后显示名称、描述、操作按钮)
- 角色互动面板
- 操作确认对话框 (用于购买、使用/丢弃物品等)

## 技术实现

### 数据保存
- 使用localStorage保存用户设置
- 动态加载和应用用户设置
- **注意：当前商店购买和背包操作仅在前端模拟，刷新页面会丢失状态。实际应用需与后端或游戏引擎集成。**

### 响应式交互
- 事件监听器处理用户输入
- 模态对话框定位和显示
- 动态更新UI内容

### UI定制化
- CSS变量实现主题定制
- 预设颜色方案
- 字体样式和大小调整
- 全局字体大小响应式缩放（支持rem单位自适应）
- 实时字体大小预览功能

### 游戏通信
- 通过格式化消息与游戏系统交互
- 请求格式：`<request:{用户指令}>`
- **新增商店购买和背包操作的游戏请求**

## 更新历史

### 2023-xx-xx 版本更新
- 初始版本发布

### 2023-xx-xx 功能更新
- 添加了弹幕系统
- 优化了地图渲染性能

### 最新版本更新
- 改进了字体大小调整功能，现在能够影响所有界面元素
- 添加了基于rem单位的响应式布局，使界面元素能够根据字体大小同步缩放
- 增加了地图标签字体动态调整功能，确保在不同字体大小下保持可读性
- 实现了字体大小滑块的实时预览功能，无需点击应用按钮即可看到效果
- 优化了SVG地图元素的文本缩放算法，防止字体过小导致的可读性问题

### 2024-04-30 弹幕功能修复
- 修复了JavaScript事件监听器中的ID引用不匹配问题：
  - 将 `send-bullet-reply-btn` 修正为 `bullet-send-btn`
  - 添加 `bullet-close-btn` 用于关闭弹幕功能
  - 添加 `bullet-chat-toggle-btn` 用于切换弹幕显示
- 添加了新的弹幕交互界面，包括开关按钮和关闭按钮
- 优化了弹幕发送功能，现在会自动显示用户自己发送的弹幕
- 这些修复解决了弹幕聊天功能中按钮无响应的问题
- 重要提示：在后续开发中，确保事件监听器的ID引用与HTML元素ID完全匹配

### 2024-05-01 添加商店和背包功能
- **添加了商店和背包系统的基本框架**
- 在顶部导航栏添加了商店和背包按钮
- 实现了商店和背包模态框的HTML结构和CSS样式
- **实现了地图数据解析 `[MONEY:]`, `[SHOP_ITEMS:]`, `[INVENTORY:]` 标签**
- 实现了商店商品列表的显示逻辑
- 实现了背包物品列表的显示逻辑
- 实现了点击背包物品显示详情和操作按钮的功能
- **实现了商店购买物品（带确认）和背包丢弃物品（带确认）的前端交互逻辑**
- 更新了文档，添加了相关功能和数据格式的说明
- **注意：物品使用功能和与游戏引擎的实际交互尚未完全实现**

## 使用方法

1. **地点导航**：
   - 从主地点列表选择一个主要地点
   - 从子地点列表选择具体位置
   - 点击"前往此处"按钮确认移动

2. **角色互动**：
   - 选择地点上的角色
   - 从预设互动选项中选择或输入自定义互动
   - 确认执行互动

3. **发送弹幕**：
   - 在弹幕输入框中输入内容
   - 点击发送按钮或按Enter键发送

4. **使用商店**：
   - 点击顶部导航栏的<i class="fas fa-store"></i>**商店按钮**打开商店
   - 查看你的金币和商店中的商品
   - 在商品下方输入购买数量（默认为1）
   - 点击**购买按钮**，在确认对话框中确认购买

5. **管理背包**：
   - 点击顶部导航栏的<i class="fas fa-briefcase"></i>**背包按钮**打开背包
   - 点击背包中的物品，下方会显示物品详情和可用操作（如"使用"、"丢弃"）
   - 点击**操作按钮**执行相应操作（丢弃会有确认提示）

6. **自定义设置**：
   - 点击设置按钮打开设置面板
   - 调整字体、颜色等设置
   - 使用字体大小滑块实时预览界面字体大小效果
   - 点击应用保存设置

## 重要Bug修复

### 2024-04-30 弹幕功能修复
- 修复了JavaScript事件监听器中的ID引用不匹配问题：
  - 将 `chat-toggle-btn` 修正为 `bullet-chat-toggle-btn`
  - 将 `close-chat-btn` 修正为 `bullet-close-btn`
  - 将 `send-bullet-reply-btn` 修正为 `bullet-send-btn`
- 这些修复解决了弹幕聊天功能中按钮无响应的问题
- 重要提示：在后续开发中，确保事件监听器的ID引用与HTML元素ID完全匹配